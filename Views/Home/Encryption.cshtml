@page
@model AnotherTechblog.Models.Encryption
@{
    /*
    bool IsPost = true;

    if (IsPost)
    {
        string Input = Request.Form["EncryptionMessage"];
        string Type = Request.Form["EncryptionType"];

        <text>
                  You entered: <br />
                  Company Name: @Input <br />
                  Contact Name: @Type <br />
        </text>
    
    }
    */
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Another Techblog</title>
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:400,700" rel="stylesheet">
    <link href="~\Content\Index.css" rel="stylesheet" type="text/css" />
    <style>
        .body {
            font-family: 'Open Sans', sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
        }

        .g1 {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            font-size: 24px;
            font-weight: 400;
            color: #666;
            margin-bottom: 40px;
            text-align: center;
        }

        .g2 {
            max-width: 300px;
            margin: 0 auto;
            padding: 20px;
            font-size: 12px;
            font-weight: 400;
            color: #666;
            margin-bottom: 40px;
            text-align: center;
        }

        .h1 {
            font-size: 48px;
            font-weight: 700;
            color: #333;
            margin-top: 100px;
            margin-bottom: 20px;
            text-align: center;
        }


        .greyBar {
            display: block;
            font-size: 18px;
            font-weight: 700;
            color: #fff;
            background-color: #333;
            padding: 20px;
            text-align: center;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease-in-out;
            -webkit-transition: background-color 0.3s ease-in-out;
            -moz-transition: background-color 0.3s ease-in-out;
            -ms-transition: background-color 0.3s ease-in-out;
            -o-transition: background-color 0.3s ease-in-out;
        }

        a:hover {
            background-color: #666;
        }
    </style>
</head>

<body class=".body">
    
    <form method="post" >
        <fieldset>
            <div class="g1">
                <h1 class=".h1">Encryptopia</h1>
                <p>All Encription Algorithms that i know are here.</p>
            </div>

            <div>
                <label for="EncryptionMessage">Message to Encrypt:</label>
                <input type="text" name="EncryptionMessage" value="" />
            </div>

            <div>
                <label for="EncryptionType">Encryption Type:</label>
                <input type="text" name="EncryptionType" value="" />
            </div>
        </fieldset>
    </form>
</body>
</html>
